# 框架设计

框架支持运行时和编译时两种场景需要。

#### 工程结构

```text
data-semantic/
├── runtime/                    # 运行时包 (data-semantic)
│   ├── src/
│   │   ├── index.js            # 入口：导出单例与类
│   │   ├── runtime.js          # 运行时核心：索引、渲染、list 支持
│   │   ├── resolveKey.js       # key 解析：绝对/相对/序号寻址
│   │   ├── list.js             # 列表渲染：模板缓存、clone、context
│   │   ├── utility.js          # 通用逻辑：路径解析、深合并、白名单
│   │   └── umd.js              # UMD 构建入口
│   ├── test/
│   │   └── runtime.test.mjs    # jsdom 单元测试
│   └── package.json
├── compiler/                   # 编译器包 (data-semantic-compiler)
│   ├── src/
│   │   ├── core.js             # 核心：parseSemanticStructure / inspect / check / compile
│   │   └── cli.js              # CLI：build / check / inspect
│   └── package.json
├── protocol/
│   └── SPEC.md                 # 协议规范（权威定义）
├── tests/
│   └── protocol-tests/         # 一致性测试
│       ├── cases/              # 测试用例（template + data + expected）
│       ├── runner-runtime.mjs  # Runtime 执行器
│       └── runner-compiler.mjs # Compiler 执行器
├── official/                   # 官网
│   ├── index.html
│   └── demo.html
├── package.json                # monorepo 根（npm workspaces）
└── 协议设计.md
```

#### 开发命令

```bash
npm run build        # 构建 ES + UMD 产物（dist/runtime.es.js / runtime.umd.js）
npm test             # 运行 jsdom 单元测试
npm run typecheck    # TypeScript 类型检查（tsc --noEmit）
npm run dev          # 启动 Vite 开发服务器
```

#### 增量渲染模型

runtime框架需要实现细粒度的增量更新。以「槽位索引 + 细粒度增量渲染」为核心：

1. **槽位索引**：首次 `render()` 时扫描作用域内全部 `data-semantic` 绑定，建立 `key → 元素列表` 索引并缓存，后续渲染不再全量扫描 DOM。
2. **细粒度渲染**：每次 `render(data)` 先对数据源做纯增量深合并（`deepMerge`，数组直接整体替换），再收集本次传入数据的全部叶子路径，仅更新这些路径对应的槽位；未涉及的槽位保持原值，互不干扰。数组收缩时补收集越界索引路径，自动清空多余槽位并告警。
3. **结构自愈**：通过 `MutationObserver` 监听作用域内的 `childList` 与 `attributes` 变化，仅当出现新增/移除的 data-semantic 绑定或 `data-semantic*` 属性变化时增量重建索引，并把数据源中已存在的值自动补渲染到新插入的绑定节点；渲染自身产生的 `textContent` / 普通属性变化会被过滤，避免无谓重建。
4. **AI 流式友好**：流式输出时数据以小块高频注入，每次 `render` 只更新注入字段对应的槽位（`O(注入字段数)`），无需传全量数据；删除字段通过显式传 `undefined` 表达（如 `{ user: { age: undefined } }`），`null` 与 `undefined` 语义一致（清空对应槽位并告警）。
