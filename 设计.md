# `data-semantic` 架构设计

data-semantic 是为AI 时代设计的UI文本渲染协议&实现框架——将UI 结构与页面数据的获取/处理逻辑解耦。纯 HTML 声明式绑定、零 JS 侵入，这对 AI 生成代码是天然友好的架构

## 一、范式优势

#### 开发体验

data-semantic的核心技术原理，是基于html原生标准的`data-*`机制，来实现数据绑定语义声明，再通过运行时进行数据注入、触发渲染。

##### 1、声明数据

声明一个key为hello的数据要求、以Node.textContent方式绑定。

```html
<div data-semantic="hello"></div>
```

##### 2、渲染数据

渲染数据时，页面须先引入运行时。

```html
<script src="https://unpkg.com/data-semantic/dist/runtime.umd.js"></script> 
```

现在，我们在业务逻辑中，调用渲染方法来实现数据渲染。

```html
<script>
    const data = {
        hello:"你好，世界"   //在结构化数据中，用相同的key提供数据
    }
    DataSemantic.render(data)  //框架会自动将数据渲染到每一个key的对应位置
    
</script> 
```

##### 3、渲染结果

```html
<div data-semantic="hello">你好，世界</div>
```

由于data-semantic属性在渲染后依然存在，实际上同时为页面保留了语义，这一点在AI时代将成为一个核心优势，你可以使用诸如`data-semantic="page.title"`、`data-semantic="project.name"`、`data-semantic="project.create_time"`、`data-semantic-alt="流程图"`之类的形式来清晰声明数据的语义。

#### 国际化场景

i18n是一个典型的应用方向，我们可以用于更优雅的实现国际化需求。

##### 传统框架

i18next框架是这样处理的

```html
<!DOCTYPE html>
<html>
<body>
    <!-- 预留id索引，与样式有耦合 -->
    <div id="hello"></div>
    <div id="desc"></div>
    <select id="lang-select">
        <option value="zh">中文</option>
        <option value="en">English</option>
    </select>

    <script src="https://unpkg.com/i18next@26.0.0/dist/umd/i18next.js"></script>
    <script>
        // 1. 初始化 i18next
        i18next.init({
            lng: 'en',
            resources: {
                zh: { translation: { 
                    hello: "你好，世界", 
                    desc: "欢迎使用 i18next" 
                } },
                en: { translation: { 
                    hello: "Hello, World", 
                    desc: "Welcome to i18next Framework" 
                } }
            }
        });

        // 2. 命令式更新 UI：必须为每个 DOM 元素手动编写更新逻辑
        function renderUI(lang) {
            i18next.changeLanguage(lang);
            document.getElementById('hello').textContent = i18next.t('hello');
            document.getElementById('desc').textContent = i18next.t('desc');
            // 如果有 10 个字段，就需要写 10 行类似的代码
        }

        renderUI('en');
        document.getElementById('lang-select').addEventListener('change', (e) => {
            renderUI(e.target.value);
        });
    </script>
</body>
</html>
```

##### data-semantic

```html
<!DOCTYPE html>
<html lang="zh-CN">
    <body>
        <!-- 预留语义索引 -->
        <div data-semantic="hello"></div>
        <select id="lang-select">
            <option value="zh">🇨🇳 简体中文</option>
            <option value="en" selected>🇺🇸 English</option>
        </select>

        <script src="https://unpkg.com/data-semantic/dist/runtime.umd.js"></script> 
        <script>
            // 定义或从外部加载多语言资源包
            const locales = {
                zh: {
                    hello: "你好，世界",
                    desc: "欢迎使用 DataSemantic 框架"
                },
                en: {
                    hello: "Hello, World",
                    desc: "Welcome to DataSemantic Framework"
                }
            };

            // 执行一次默认渲染
            const langSelect = document.getElementById('lang-select');
            let currentLang = event.target.value;
            DataSemantic.render(locales[currentLang]) 

            // 绑定点击事件，方便用户触发新的渲染
            languageToggleBtn.addEventListener('click', () => {
                currentLang = event.target.value;
                DataSemantic.render(locales[currentLang]);
            });
        </script> 
	</body>
</html>
```

可以看出，data-semantic方案有诸多优势：

- ✅ HTML中**声明式**绑定，一看就知道哪些地方需要数据
- ✅ runtime**自动批量渲染**，新增字段无需修改JS胶水代码
- ✅ 数据和视图**解耦**，UI结构由AI或设计师维护，数据由开发者或CMS提供

#### AI原生优势

data-semantic并非一个更好的i18n框架，从范例来看，data-semantic真正的关键范式改变是实现了一个纯粹的声明式数据绑定协议——纯 HTML 声明式绑定、零 JS 侵入，这对 AI 生成代码是天然友好的架构。

相比传统命令式范式框架，实现了关注点的彻底分离。

##### UI 的复杂度低

AI只需生成纯粹的、带语义声明的 HTML 结构，这是它的强项，新增字段只需在 HTML 中添加一个 `data-semantic` 属性即可，无需编写任何 JavaScript 绑定代码。

##### 实现了协作解耦

设计端、后端、前端角色可以并行工作，互不阻塞

* 数据侧（如 i18n 文件、API 响应）：可独立增删改字段，完全不影响 UI 结构
* UI 侧（HTML 模板）：可自由调整布局和样式，无需担心破坏数据绑定

##### 测试成本趋近于零

传统框架的 UI 测试需要模拟 DOM 环境、处理异步渲染、管理组件状态，测试成本高昂。data-semantic可以双端独立单元测试：

- UI侧：HTML 是原生标准，无需任何框架环境，直接验证视觉风格和响应式布局

- 数据侧：数据源是纯 JSON，可在 Node.js 环境中独立进行单元测试

- 集成测试：两者通过 `DataSemantic.render()` 连接，这个桥梁的逻辑足够简单，几乎无需测试

##### 保留语义

这为三方精准分析语义提供了想象空间。

data-semantic框架在加载时，会自动注入协议规定的head信息，向三方平台声明，本页面的数据遵循了语义声明

```html
<head>
    <!-- 声明页面遵循 data-semantic 协议 -->
    <meta name="data-semantic" content="1.0" />
    <!-- 或者更详细的声明 -->
    <meta name="semantic-ui" content="protocol=data-semantic; version=1.0; runtime=data-semantic" />
</head>
```



## 二、data-semantic协议

data-semantic需要实现一个声明式数据绑定协议，这个协议我们称之为data-semantic。

#### 声明方式

```html
<h1 data-semantic="page.title">Default Title</h1>
```

协议规定了2种声明机制：

- `data-semantic={key}`， 对应了 DOM 的 `Node.textContent`。
- `data-semantic-{attr}={key}`， 对应了 DOM 的 `Element.setAttribute()`。

#### key路径

`data-semantic` 中的 `key` 路径遵循的就是标准的 JSON 寻址（JSON Path / Dot Notation），这与 JavaScript 中直接访问对象属性的方式完全一致。

比如在html中使用`data-semantic="page.title"`声明数据，数据侧就这样提供数据

```js
let data = {
    page:{
        title:"这是页面标题"
    }
}
DataSemantic.render(data)
```

#### 范围约束

其中`data-semantic-<attr>` 的 `<attr>` 必须落在白名单内，否则视为无效绑定告警。协议规定，所有事件处理器、style、全局标识符、iframe等内联脚本均不允许进行渲染，目前框架支持如下几种白名单属性。

#### 渲染规范

##### 1. `data-semantic`（基础文本内容）

绑定 `Node.textContent`，用于普通文本展示。

```html
<h1 data-semantic="user.name">默认姓名</h1>
<p data-semantic="user.title">默认职位</p>

<script src="https://unpkg.com/data-semantic/dist/runtime.umd.js"></script>
<script>
    const data = { user: { name: "张三", title: "高级前端工程师" } };
    DataSemantic.render(data);
    // 渲染后：
    // <h1 data-semantic="user.name">张三</h1>
</script>
```

##### 2. `data-semantic-placeholder`（输入框占位符）

绑定 `placeholder` 属性，适用于 `<input>`、`<textarea>`。

```html
<input data-semantic-placeholder="user.placeholder" placeholder="默认占位文本" />

<script>
  const data = { user: { placeholder: "请输入搜索关键词" } };
  DataSemantic.render(data);
  // 渲染后：
  // <input data-semantic-placeholder="user.placeholder" placeholder="请输入搜索关键词" />
</script>
```

##### 3. `data-semantic-value`（表单默认值）

绑定 `value` 属性，用于单向设置表单控件的初始值（**注意**：不参与双向绑定，仅用于初始渲染）。

```html
<input type="text" data-semantic-value="user.defaultValue" value="旧值" />

<script>
  const data = { user: { defaultValue: "初始值" } };
  DataSemantic.render(data);
  // 渲染后：
  // <input type="text" data-semantic-value="user.defaultValue" value="初始值" />
</script>
```

##### 4. `data-semantic-title`（鼠标悬停提示）

绑定 `title` 属性，为任意元素添加原生悬浮提示。

```html
<button data-semantic-title="user.bio" title="默认提示">悬停查看</button>

<script>
  const data = { user: { bio: "专注AI与UI融合" } };
  DataSemantic.render(data);
  // 渲染后：
  // <button data-semantic-title="user.bio" title="专注AI与UI融合">悬停查看</button>
</script>
```

##### 5. `data-semantic-alt`（图片无障碍替代文本）

绑定 `alt` 属性，用于 `<img>` 等媒体元素，提升 SEO 和无障碍访问。

html

```html
<img data-semantic-alt="user.name" alt="默认头像" src="https://i.pravatar.cc/150?img=11" />

<script>
  const data = { user: { name: "张三" } };
  DataSemantic.render(data);
  // 渲染后：
  // <img data-semantic-alt="user.name" alt="张三" src="https://i.pravatar.cc/150?img=11" />
</script>
```

##### 6. `data-semantic-src`（动态资源链接）

绑定 `src` 属性，用于 `<img>`、`<video>`、`<audio>`、`<iframe>` 等。

```html
<img data-semantic-src="user.avatar" src="default.jpg" />

<script>
  const data = { user: { avatar: "https://i.pravatar.cc/150?img=11" } };
  DataSemantic.render(data);
  // 渲染后：
  // <img data-semantic-src="user.avatar" src="https://i.pravatar.cc/150?img=11" />
</script>
```

##### 7. `data-semantic-href`（动态链接地址）

绑定 `href` 属性，用于 `<a>` 标签动态生成跳转链接。

```html
<a data-semantic-href="user.profileLink" href="#">查看个人主页</a>

<script>
  const data = { user: { profileLink: "/profile/zhangsan" } };
  DataSemantic.render(data);
  // 渲染后：
  // <a data-semantic-href="user.profileLink" href="/profile/zhangsan">查看个人主页</a>
</script>
```

##### 8. `data-semantic-aria-label`（无障碍标签）

绑定 `aria-label` 属性，为无文本交互元素提供屏幕阅读器标签。

```html
<button data-semantic-aria-label="user.name" aria-label="默认用户">
  <svg><!-- 纯图标按钮 --></svg>
</button>

<script>
  const data = { user: { name: "张三" } };
  DataSemantic.render(data);
  // 渲染后：
  // <button data-semantic-aria-label="user.name" aria-label="张三">...</button>
</script>
```

##### 9. `data-semantic-aria-description`（无障碍详细描述）

绑定 `aria-description` 属性，为复杂组件提供额外描述。

```html
<div role="tooltip" data-semantic-aria-description="user.bio" aria-description="默认描述">
  悬停查看详情
</div>

<script>
  const data = { user: { bio: "专注AI与UI融合" } };
  DataSemantic.render(data);
  // 渲染后：
  // <div role="tooltip" data-semantic-aria-description="user.bio" aria-description="专注AI与UI融合">悬停查看详情</div>
</script>
```

##### 10. `data-semantic-content`（元数据动态内容）

绑定 `content` 属性，专用于 `<meta>` 标签，动态设置页面描述或关键词。

```html
<meta name="description" data-semantic-content="user.metaDesc" content="默认描述" />

<script>
  const data = { user: { metaDesc: "Data-Text 完整属性范例页面，展示所有白名单属性" } };
  DataSemantic.render(data);
  // 渲染后：
  // <meta name="description" data-semantic-content="user.metaDesc" content="Data-Text 完整属性范例页面，展示所有白名单属性" />
</script>
```

##### 11. `data-semantic-data-*`（自定义 data 属性透传）

允许通过 `data-semantic-data-*` 将数据渲染到任意 `data-*` 自定义属性，格式为 `data-semantic-data-<自定义名>`。

```html
<div data-semantic-data-user-id="user.id" data-user-id="默认ID">用户卡片</div>

<script>
  const data = { user: { id: "12345" } };
  DataSemantic.render(data);
  // 渲染后：
  // <div data-semantic-data-user-id="user.id" data-user-id="12345">用户卡片</div>
</script>
```



## 三、框架设计

框架支持运行时和编译时两种场景需要。

#### 渲染模型（细粒度增量）

运行时以「槽位索引 + 细粒度增量渲染」为核心：

1. **槽位索引**：首次 `render()` 时扫描作用域内全部 `data-semantic` 绑定，建立 `key → 元素列表` 索引并缓存，后续渲染不再全量扫描 DOM。
2. **细粒度渲染**：每次 `render(data)` 先对数据源做纯增量深合并（`deepMerge`，数组直接替换），再收集本次传入数据的全部叶子路径，仅更新这些路径对应的槽位；未涉及的槽位保持原值，互不干扰。
3. **结构自愈**：通过 `MutationObserver` 监听作用域内的 `childList` 与 `attributes` 变化，仅当出现新增/移除的 data-semantic 绑定或 `data-semantic*` 属性变化时增量重建索引；渲染自身产生的 `textContent` / 普通属性变化会被过滤，避免无谓重建。
4. **AI 流式友好**：流式输出时数据以小块高频注入，每次 `render` 只更新注入字段对应的槽位（`O(注入字段数)`），无需传全量数据；删除字段通过显式传 `undefined` 表达（如 `{ user: { age: undefined } }`）。

#### 工程结构

```text
data-semantic/
├── src/
│   ├── index.js         
│   ├── utility.js        # 通用逻辑
│   ├── runtime.js        # 运行时能力
│   └── compiler.js       # 编译时能力
├── package.json
└── vite.config.js
```

