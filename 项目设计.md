# 项目设计

我希望将 `data-semantic` 定位是领域驱动设计（DDD）的渲染层。

## 一、工程结构

不要把 Compiler 的逻辑直接塞进现有 Runtime。

改成：

```
data-semantic
├── runtime    //运行时项目，最终npm发布到data-semantic包
├── compiler   //编译时项目，最终npm发布到data-semantic-compiler包
├── protocol   //语义协议标准
└── tests      //共享测试
```

Runtime 和 Compiler 可以独立演进，但协议必须统一

Runtime 和 Compiler 必须遵循完全一致的：

- semantic key 解析规则
- attribute semantic 规则
- list 规则
- relative key 规则
- 数据路径规则
- 缺失数据规则
- 嵌套 list 规则

不要通过共享大量实现代码保证一致性。

应该通过：

```
Protocol Specification
+
Conformance Tests
```

保证一致性。最终要求：

```
同一 HTML
+
同一 Data
```

Runtime 和 Compiler 必须得到语义一致的结果。

## 二、协议新增：

#### List Semantic

新增：

```
data-semantic-list="blog_domain.list"
```

List 的核心语义

`data-semantic-list` 所在元素是列表容器，不参与复制。其innerHTML就是：List Item Template。

例如：

```html
<div data-semantic-list="blog_domain.list" class="blog-grid">
  <div class="card">
    <h2 data-semantic=".title"></h2>
    <span data-semantic=".readTime"> min read</span>
  </div>
</div>
```

数据：

```json
{
  "blog_domain": {
    "list": [
      {
        "title": "AI 入门",
        "readTime": 3
      },
      {
        "title": "Vue 进阶",
        "readTime": 5
      }
    ]
  }
}
```

渲染结果：

```html
<div class="blog-grid">
  <div class="card">
    <h2>AI 入门</h2>
    <span>3 min read</span>
  </div>
  <div class="card">
    <h2>Vue 进阶</h2>
    <span>5 min read</span>
  </div>
</div>
```

#### 相当寻址

为了配合list这样的容器语法，不再严格遵循json path规范，而是新增以 `.` 开头的相对语义的key语法：

```
data-semantic=".title"
data-semantic=".author.name"
```

含义从当前 semantic context 开始进行相对寻址。

例如：

```
<div data-semantic-list="blog_domain.list">
  <h2 data-semantic=".title"></h2>
  <span data-semantic=".author.name"></span>
</div>
```

`.title`解析，会基于列表数据的位置序号，实际自动对应`blog_domain.list[i].title`

#### 序号寻址

支持直接使用`[]`语法来表示数组的下标寻址，但是我们建议直接优先使用`·`前缀相对寻址，因为直观。

老版本采用data-semantic="pages.list.0.name" 这种数组下标寻址方式，直接废弃，不做向后兼容。



## 三、Compiler

新增 Compiler。

输入：`template html`+`json data`

输出：`html`

Compiler直接理解 semantic protocol 并进行静态展开。

#### Core 与 CLI 分离

Compiler 内部分为：

* Compiler Core
* CLI层

Compiler Core 提供程序协议API：

```
compile(template, data, options)
```

CLI 负责调用core，对外提供编译时指令：

```
data-semantic build
data-semantic check
data-semantic inspect
```

### inspect能力

让 AI 理解 Semantic HTML：

```
template
 ↓
semantic structure
```

输出应该能告诉 AI：

```
semantic keys
list nodes
relative keys
data dependencies
```

### check能力

不生成最终页面，只检查协议和数据：

- semantic key 是否存在
- list source 是否为 collection
- relative key 是否有合法 context
- 嵌套 list 是否正确
- 数据类型是否合理
- HTML semantic declaration 是否合法

AI 可以：

```
edit
→ check
→ fix
→ check
→ build
```

### build

build负责真正编译：

## 三、Conformance Test

这是本次改造非常重要的一部分。

不要只写 Runtime Test 和 Compiler Test。

应该建立协议级测试：

```
protocol-tests/
```

每个 case 包含：

```
template
data
expected
```

例如：

```
case/list-basic/
├── template.html
├── data.json
└── expected.html
```

测试要求：

```
Runtime semantic result
        ≈
Compiler result
```

至少覆盖：

```
absolute key
relative key
simple list
empty list
nested list
nested relative key
attribute binding
missing key
multiple semantic bindings
```

以后每增加一个协议能力，都必须同时更新：

```
Protocol Spec
Conformance Test
Runtime
Compiler
```

这样可以避免 Runtime 和 Comptime 漂移。

