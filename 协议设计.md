# 《data-semantic设计》

data-semantic首先是一个html语义插座协议，其次才是runtime和comptime实现。

## 一、协议1.0

data-semantic需要实现一个声明式数据绑定协议。

#### 声明方式

当前支持三种基础的正交语义声明需求：填什么内容、填到哪里、要不要显示。

| 声明                         | 对应操作               |
| ---------------------------- | ---------------------- |
| data-semantic={key}          | Node.textContent       |
| data-semantic-{attr}={key}   | Element.setAttribute() |
| data-semantic-display={key} | Element.style.display  |
| data-semantic-list={key}     | 列表容器（innerHTML 为模板，n 次实例化） |

其中`data-semantic-<attr>` 的 `<attr>` 必须落在白名单内，否则视为无效绑定告警。协议规定，所有事件处理器、style（唯一例外：`data-semantic-display` 仅允许控制 `Element.style.display`）、全局标识符、iframe等内联脚本均不允许进行渲染，目前框架支持如下几种白名单属性：placeholder、value、title、alt、src、href、aria-label、aria-description、content、data-*

示例

```html
<h1 data-semantic="page.title">Default Title</h1>
<input data-semantic-placeholder="form.placeholder">
<h1 data-semantic-display="user.isVip">
```

#### 列表语义

`data-semantic-list` 声明列表容器，其 innerHTML 为列表项模板。渲染时对数组做 n 次实例化，替换容器内容：

```html
<div data-semantic-list="posts">
  <article>
    <h2 data-semantic=".title"></h2>
    <span data-semantic=".author"></span>
  </article>
</div>

<script>
  const data = {
    posts: [
      { title: "AI 入门", author: "Alice" },
      { title: "Vue 进阶", author: "Bob" }
    ]
  };
  DataSemantic.render(data);
  // 渲染后容器内有两个 <article>，分别绑定 posts[0] 和 posts[1] 的数据
</script>
```

列表容器规则：
- 容器元素自身不复制，只替换其子内容
- `.title` 相对 key 相对最近的 list context（即 `posts[i].title`）
- 空数组 `[]` 清空容器，不告警
- list source 必须是数组，否则协议违规（check 报错，runtime 告警跳过）
- 支持嵌套 list：内层 `.items` 相对 `categories[i].items`

#### 寻址方式

UI侧声明的槽位值，对应的是数据侧的key路径。

##### 绝对 key

标准 Dot Notation，从数据根寻址。支持嵌套与数组索引：

```html
<p data-semantic="page.title"></p>
<p data-semantic="user.addresses.0.city"></p>
```

##### 相对 key

以 `.` 开头，相对最近的 list context 寻址：

```html
<div data-semantic-list="posts">
  <h2 data-semantic=".title"></h2>           <!-- posts[i].title -->
  <span data-semantic=".author.name"></span>  <!-- posts[i].author.name -->
</div>
```

##### 序号寻址（[] 括号语法）

支持 `[]` 括号语法进行数组下标寻址（保留但非推荐，建议优先使用相对寻址）：

```html
<div data-semantic-list="items">
  <p data-semantic=".list[0].name"></p>  <!-- items[i].list[0].name -->
</div>
```

> ⚠️ 老版本采用 `pages.list.0.name` 这种点数字下标方式已废弃，请使用相对寻址或 `[]` 括号语法。

相应的，也会引入数据规范约束：数据侧的key不应该包含 `.`，因为会与路径分隔符冲突，造成寻址问题。

#### 语义化要求

runtime在加载时，应该自动向页面 `<head>` 注入语义协议信息，向三方开放声明本页面遵循data-semantic协议规定的语义化声明：

```html
<meta name="data-semantic" content="1.0" />
```

开发者对槽位（key）的命名，应该遵循语义化表示，尽可能避免纯id式的声明

```html
<p data-semantic="abcd123"></p>
<div data-semantic="ccabdd3c-f196-4c1f-935b-c32bc53fa162"></div>
```

#### 渲染方式

由runtime根据协议规定的声明语义，将开发者传入的实际数据进行对应的渲染。

runtime只需要暴露一个至简的`render(data)`，要求开发者传入一个合法的json即可，。

#### 数据边界

在数组场景，runtime和开发者应该遵循以下明确边界，避免对框架协议行为的误解：

##### 1、 数组必须整体传入

对于数组，`deepMerge`不会递归合并 ，而是直接整体替换，因此每次传入数组都必须是完整数组：

比如数组有两个值，你需要更新第二个。

* ✅ 正确方式

```js
render({ pages: { list: ['a', 'b'] } });
```

* ❌ 错误方式

```js
render({ pages: { list: [undefined, 'x'] } });
```

##### 2、key必须是标量

* ✅ 正确方式

```html
<div data-semantic="pages.list.0.name"></div>
```

* ❌ 错误方式

```html
<div data-semantic="pages.list"></div>
```

绑定到对象/数组的槽位会被跳过（不渲染、开发环境告警）。



## 二、框架设计

框架支持运行时和编译时两种场景需要。

#### 工程结构

```text
data-semantic/
├── runtime/                    # 运行时包 (data-semantic)
│   ├── src/
│   │   ├── index.js            # 入口：导出单例与类
│   │   ├── runtime.js          # 运行时核心：索引、渲染、list 支持
│   │   ├── resolveKey.js       # key 解析：绝对/相对/序号寻址
│   │   ├── list.js             # 列表渲染：模板缓存、clone、context
│   │   ├── utility.js          # 通用逻辑：路径解析、深合并、白名单
│   │   └── umd.js              # UMD 构建入口
│   ├── test/
│   │   └── runtime.test.mjs    # jsdom 单元测试
│   └── package.json
├── compiler/                   # 编译器包 (data-semantic-compiler)
│   ├── src/
│   │   ├── core.js             # 核心：parseSemanticStructure / inspect / check / compile
│   │   └── cli.js              # CLI：build / check / inspect
│   └── package.json
├── protocol/
│   └── SPEC.md                 # 协议规范（权威定义）
├── tests/
│   └── protocol-tests/         # 一致性测试
│       ├── cases/              # 测试用例（template + data + expected）
│       ├── runner-runtime.mjs  # Runtime 执行器
│       └── runner-compiler.mjs # Compiler 执行器
├── official/                   # 官网
│   ├── index.html
│   └── demo.html
├── package.json                # monorepo 根（npm workspaces）
└── 协议设计.md
```

#### 开发命令

```bash
npm run build        # 构建 ES + UMD 产物（dist/runtime.es.js / runtime.umd.js）
npm test             # 运行 jsdom 单元测试
npm run typecheck    # TypeScript 类型检查（tsc --noEmit）
npm run dev          # 启动 Vite 开发服务器
```

#### 渲染模型

框架需要实现细粒度的增量更新。以「槽位索引 + 细粒度增量渲染」为核心：

1. **槽位索引**：首次 `render()` 时扫描作用域内全部 `data-semantic` 绑定，建立 `key → 元素列表` 索引并缓存，后续渲染不再全量扫描 DOM。
2. **细粒度渲染**：每次 `render(data)` 先对数据源做纯增量深合并（`deepMerge`，数组直接整体替换），再收集本次传入数据的全部叶子路径，仅更新这些路径对应的槽位；未涉及的槽位保持原值，互不干扰。数组收缩时补收集越界索引路径，自动清空多余槽位并告警。
3. **结构自愈**：通过 `MutationObserver` 监听作用域内的 `childList` 与 `attributes` 变化，仅当出现新增/移除的 data-semantic 绑定或 `data-semantic*` 属性变化时增量重建索引，并把数据源中已存在的值自动补渲染到新插入的绑定节点；渲染自身产生的 `textContent` / 普通属性变化会被过滤，避免无谓重建。
4. **AI 流式友好**：流式输出时数据以小块高频注入，每次 `render` 只更新注入字段对应的槽位（`O(注入字段数)`），无需传全量数据；删除字段通过显式传 `undefined` 表达（如 `{ user: { age: undefined } }`），`null` 与 `undefined` 语义一致（清空对应槽位并告警）。
